---
topic: HeAgent MCP Client 集成 V2 — 写操作治理 + Resources/Prompts 原语
updated: 2026-07-17T14:40
---

- (decision) P1 定稿=Resources on-demand 内置工具(list_resources/read_resource)，不自动全量注入（规避 R3 上下文侵蚀）
- (decision) P2 定稿=Prompts CLI slash 表面(/mcp-prompt server name args) 渲染注入 user message
- (decision) P4 定稿=缺 annotations→fail-safe 视为非只读需确认(APPROVAL_REQUIRED)，保守不放行 destructive（OQ-1 待 review 保守度）
- (assumption) annotations 用 HeAgent 自有 Pydantic 模型，不直接依赖 mcp.types 进 types.py（避免 types 层上浮 mcp 依赖，保 DAG）
- (assumption) 显式策略(approval_tools/approval_mcp_tools)优先级高于 annotation；用户可强制全 MCP 需确认覆盖 server readOnly 声明（OQ-3 待确认）
- (event) PRD draft 完成（Fast path）：3 feature(Epic A/B/C) + FR-A*/B*/C* + 5 OQ + 零回归/治理确定性 SM；接入点实现留架构(OQ-2/4)
- (change) Finalize 修复 reviewer 5 findings：P4 矛盾→定稿(删待review) / 删 FR-B5(deferred non-goal 重复) / FR-A4 显式策略优先定稿 / FR-A2 默认→保守标记 / SM-4 内置工具数 18→19；补 reconcile gap resource templates→OQ-6
- (event) PRD finalized（status: final）；OQ-2/4/6 真开放留架构，OQ-1/3 已定稿留档；polish 自审（控预算）；下一步 bmad-architecture
- (decision by architect) OQ-2 定稿=annotations 经 ToolSchema 显式 kwarg 注入 evaluate_tool_call（caller tool_execution.py 已解析 loop.registry.get_schema）；否决 (a)污染ToolCall (b)注入registry(违PolicyEngine无状态+registry随server连接/断连变) (c)塞RunContext.metadata(annotations是工具定义元数据非per-run授权，类别错配)
- (decision by architect) OQ-4 定稿=CLI REPL 当前无 slash 机制（input() 直送 run_stream）；新增最小 slash 分发器夹在 input() 与 run_stream 之间，/mcp-prompt 为首条命令，需持 manager 引用调用 list_prompts/get_prompt 并把渲染文本作为 user message 走 run_stream
- (decision by architect) OQ-5 定稿=subscribe_resource defer；与 FR-B3 on-demand(自动推资源更新进上下文=R3侵蚀)及 2026-07-28 stateless 迁移(订阅本质有状态)双冲突；V2 仅发现+按需读
- (decision by architect) OQ-6 定稿=resource templates(list_resource_templates)defer；具名 list_resources/read_resource 已覆盖主用例，模板需额外 URI 展开逻辑，边际价值低
- (decision by architect) 新增承重决策(ADR)=MCPClientManager 须持有 server→session 映射供 Resources/Prompts 复用；V1 session 仅存活于 _server_loop task 内、被 tool 闭包捕获、manager 不可达——Epic B/C 前置：_server_loop 把已 initialize 的 session 登记进 manager._sessions，断连/unregister 时摘除
- (decision by architect) list_resources/read_resource 由 MCPClientManager 在 MCP 活跃时注册为聚合桥接工具(跨 server 聚合，闭包捕获 _sessions)，复用 bridge_result 注入围栏；非 tools/builtins 静态 @tool 注册——与 V1 MCP 工具同注册路径，随 MCP 生命周期出现/消失(无 MCP=纯内置模式不注册)；PRD 称'内置工具'实为'LLM 可见工具'，语义不冲突
- (assumption by architect) build-substrate 单一交付物：solo dev tool + brownfield + 既有 frame.md 架构权威，本 spine 仅承重决策不重述既有架构；无需额外 human-facing deck/C4
- (constraint by architect) 继承基线不变量：tools/mcp 禁从 agent 导入(DAG)；执行链 PolicyEngine.evaluate→ToolExecutor→SafetyGuard→handler 固定不变；跨模块数据用 Pydantic；annotations 模型为 HeAgent 自有(不上浮 mcp 依赖进 types)
- (event by architect) Reviewer Gate 完成：lint 0 findings；3 路并行 reviewer(reality-check/rubric/adversary) 均 PASS-WITH-FINDINGS，结构经代码核实无虚构；CRITICAL+HIGH 已应用修复
- (decision by architect) Gate 修复 AD-2(CRITICAL)=fail-safe 仅 MCP 工具触发：增步 0 前置闸门(schema=None→跳过注解裁决回既有路径)，杜绝 19 内置工具被当'缺信号'强制审批(SM-4 零回归崩)；注解裁决前提=_is_mcp_tool 且 annotations 存在
- (decision by architect) Gate 修复 AD-5(HIGH)=Resources 桥接工具命名 mcp__list_resources/mcp__read_resource(mcp 聚合 token→_is_mcp_tool 识别→继承全量 V1 MCP 门控 block/approval/sandbox_mcp_tools+__mcp__ 授权)；自声明 readOnlyHint=True 默认放行；LLM 签名钉死 read_resource(server,uri) server 必填(收紧 PRD FR-B2)
- (decision by architect) Gate 修复 AD-6/AD-7(HIGH)=注入围栏提升为 mapping.guard_content 公共函数(bridge_result/read_resource/slash 共用单一实现)；纠正事实:_mcp_lifecycle 非 _build_mcp 且 manager 被 async with 包裹不可达→/mcp-prompt 推荐接入(i) manager 绑定变量经 as 进 REPL；slash 分发器渲染文本先经 guard_content 再注入
- (decision by architect) Gate 修复 AD-4(HIGH)=_sessions 单属主纪律:session 唯一属主仍是 _server_loop task，_sessions 是只读查找表；断连 flag-before-pop 先摘键再退 transport；桥接调用见键移除→规范化 ToolError(MCP server disconnected)，禁裸 KeyError/None
- (decision by architect) Gate 修复(LOW/MED):ToolAnnotations 第5字段 title 不透传(非裁决信号丢弃)；SM-4 补进 binds+Capability Map；ledger 缓存命中复核+annotation 收紧的 story 测试覆盖留下游
- (event by architect) spine finalized（ARCHITECTURE-SPINE.md status:final）；Reviewer Gate 通过；下一步=bmad-create-epics-and-stories（Epic A→B→C 分阶段）
- (event) epics.md finalized（status:final，step-01~04 全完成）：3 epic / 11 story（A=4 / B=4 / C=3）/ 16 FR 全覆盖（FR-A1~A7/FR-B1~B4/FR-C1~C4）。Story 落点严依 AD-1~8；FR-A7 跨 A.1存储+A.2不裁决；guard_content 提取落 B.3（首个消费者）；每 epic 收尾一个安全声明 story（AD-8/SM-6）。无 starter template（brownfield）。依赖链严格单向（C→B 文档化递进）。下一步=bmad-create-story Epic A.1 起逐 story（一会话一 story 控预算）
- (event) sprint-status.yaml 生成（bmad-sprint-planning）：Epic A/B/C → 主线 epic 14/15/16（延续 V1 周期 11-13 约定——skill 校验 story key 须匹配 epic-N 数字模式，禁字母/mcp 前缀，且避撞主线 epic-1/2/3）；3 epic / 11 story（14=4 / 15=4 / 16=3）/ 3 retro，全 backlog/optional（新周期未动工）；story_location=_bmad-output/mcp-client-v2/stories（与 V1「直接基于 AC 实现」不同，本轮用 bmad-create-story 逐个建文件）；yaml 结构校验通过（17 keys 合法）。下一步=bmad-create-story 14-1-annotations-data-pipeline
