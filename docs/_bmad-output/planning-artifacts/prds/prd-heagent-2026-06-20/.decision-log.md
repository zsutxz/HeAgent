# Decision Log — HeAgent MCP Client 集成 PRD

> PRD 决策审计记录。继承自 brief 的 D1-D7（见 `../../briefs/brief-heagent-2026-06-20/.decision-log.md`）；本文件记录 PRD 阶段新增决策与 review 改动。

## 继承自 Brief（D1-D7）

- D1 通用 MCP client（否决 GitHub 专用）
- D2 只接 Tools 原语
- D3 V1 只读优先，写操作进 roadmap
- D4 自用为主 + 开源友好
- D5 pin `mcp>=1.27,<2`，握手封装内部
- D6 声明式 mcpServers 配置
- D7 MCP server 归入既有不可信边界

## 2026-06-20 · PRD Create

- **工作模式**：Fast path（输入充分：brief 已定方向 / 范围 / 受众 / 成功标准，无需重复 brain dump）。
- **FR 编号策略**：FR-1 ~ FR-11 全局稳定编号，按 feature 分组（连接生命周期 / 发现桥接 / 配置 / GitHub 验收 / 安全），下游 epics 直接引用。
- **能力导向**：FR 表述能力而非实现；实现细节（`AsyncExitStack` / `ClientSession` / transport 机制）放 addendum / architecture。
- **UJ 处理**：技术 PRD，UJ downscale 为单个示例会话（不走完整 UJ-N）。
- **[ASSUMPTION] gap**：6 条，留待 finalize step 4 triage（配置落点 / 鉴权 / 验收清单 / SafetyGuard / server 形态 / 记忆整合边界）。

### DP-1 · 配置落点 = 项目根 `.mcp.json`（1/6 已定）
- **决定**：MCP server 配置文件为项目根 `.mcp.json`。
- **否决**：`.heagent/mcp.json`（`.heagent/` 现装运行时产物 skills / memory / sessions / cron，配置性质不同，混放别扭）。
- **理由**：对齐 Claude Code / Cursor 业界标准文件名与位置（brief D6），用户心智复用、开源友好度最高。修正 PRD 初稿 [ASSUMPTION] 的 `.heagent/mcp.json` 默认。

### DP-2 · 鉴权 = GitHub PAT 走环境变量（2/6）
- **决定**：GitHub 鉴权用 Personal Access Token，通过环境变量 `${GITHUB_TOKEN}` 插值注入 `.mcp.json`，不落配置明文。
- **否决**：GitHub App（V1 过重）/ 配置内明文（安全）。
- **理由**：与 HeAgent 现有 `.env` + pydantic-settings 凭据惯例一致；最小可行。

### DP-3 · GitHub 验收清单 = 列 open issue + 代码搜索（3/6）
- **决定**：V1 验收跑通两类只读 E2E：列指定 repo 的 open issue、代码搜索；其余只读为加分项。
- **理由**：两类覆盖「结构化列表」与「全文检索」两种典型调用形态，足以证明链路通；不追求工具全集覆盖。

### DP-4 · SafetyGuard 对 MCP = 声明为主（4/6）
- **决定**：V1 不扩展 `SafetyGuard` 到 MCP 工具调用；以安全声明 +「返回内容视为不可信」边界为主，检查机制扩展交 architecture 探索。
- **理由**：`SafetyGuard` 现主要管 shell 命令；MCP 工具非 shell，V1 先定边界，机制设计留 architecture。

### DP-5 · 验收 server = 官方远程 Streamable HTTP（5/6）
- **决定**：GitHub 验收用官方远程 server（`github/github-mcp-server` Streamable HTTP），不依赖本地 Docker。
- **否决**：社区 stdio 版（需本地装、维护弱）。
- **理由**：只读 + 官方维护，配置最简；双 transport 仍都支持（FR-1）。

### DP-6 · 记忆整合 = Vision 非 V1（6/6）
- **决定**：MCP 工具使用沉淀成技能的整合列为 Vision，不进 V1。
- **理由**：继承 brief 差异化定位；V1 聚焦基础接入闭环。

---

## 2026-06-20 · Finalize

- **Reviewer gate**：跑 validation rubric walker（`review-rubric.md`，verdict「需修复后通过」）+ 综合 editorial。
- **采纳并 apply**：§8 标题修正（`[ASSUMPTION]` 残留矛盾）、FR-9 schema 锚点 + V1 边界化、FR-3 区分连接失败 vs 运行时断连、FR-2 工具发现时序约束（eager/lazy 交架构定）、FR-7 无配置时行为、NFR-2 覆盖率基线、NFR-4 可测阈值、NFR-3 成功标准句。
- **未采纳（低优先，不阻塞）**：R3 用词口语化、§6 与 FR-7 冗余、§1 目标句拆分等纯文字优化。
- **结果**：`prd.md` `status: final`，无 phase-blocker，可进架构（`bmad-create-architecture`）。
